import Theme from '../style/theme';

const DefaultTheme: AnnaNative.Theme = {
  dark: false,
  roundness: 0,
  colors: {
    primary: Theme.$brandColor,
    contrastive: Theme.$contrastiveColor,
  },
};

export default DefaultTheme;
