import { StyleSheet } from 'react-native';

const styles = StyleSheet.create<any>({
  'native-picker': {},
  'native-picker-container': {
    flexDirection: 'row',
    alignItems: 'center',
  },
  'native-picker-column': {
    flex: 1,
  },
});

export default styles;
