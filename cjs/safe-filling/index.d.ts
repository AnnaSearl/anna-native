import * as React from 'react';
import { ViewStyle } from 'react-native';
export interface SafeFillingProps {
    bottom?: boolean;
    backgroundColor?: string;
    style?: ViewStyle;
}
declare const SafeFilling: React.FC<SafeFillingProps>;
export default SafeFilling;
